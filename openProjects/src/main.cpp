#include <cstdlib>
#include <iostream>
#include <ctime>

using namespace std;

int main() 
{
    int decisionVariable;
    bool decision = true;
    while (decision == true)
    {
        cout << "enter a number 0-10" << endl;
        cin >> decisionVariable;
        switch (decisionVariable)
        {
            case 0: 
                break;
            case 1: 
                break;
            default:
                break;
        }
        
    }
}